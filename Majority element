https://www.geeksforgeeks.org/problems/majority-element-1587115620/1?page=1&category=Arrays&sortBy=submissions

Given an array arr[]. Find the majority element in the array.
If no majority element exists, return -1.

This is my solution for the given problem with 1113 / 1131 test cases passed. 
Will try to reduce time complexities and all test case passed.

class Solution {
    int majorityElement(int arr[]) {
        // code here
        int n =arr.length;
        int max =0;
        for(int i=0; i<arr.length; i++){
            int count =0;
            for(int j=0;j<arr.length; j++){
                if(arr[i] == arr[j])
                count++;
            }
            max=arr[i];
            if(count>n/2){
            return max;
        }
        }
        return -1;
    }
}

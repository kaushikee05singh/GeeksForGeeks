https://www.geeksforgeeks.org/problems/first-repeating-element4018/0

Given an array arr[], find the first repeating element. 
The element should occur more than once and the index of its first occurrence should be the smallest.

Below is the solution for the same question with all test case passed.

class Solution {
    public static int firstRepeated(int[] arr) {
        // code here
        for(int i =0; i<arr.length; i++){
            for(int j = 0; j<arr.length; j++){
                if(arr[i]==arr[j] && i!=j)
                return i+1;
            }
        }
        return -1;
    }
}
